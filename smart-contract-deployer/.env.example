# 🌐 API Configuration
VITE_API_URL=http://localhost:3004

# 🔐 SECURITY - Master API Key (same as backend)
# This should match the MASTER_API_KEY in the backend .env file
# Generate with: node -e "const crypto = require('crypto'); console.log('cfio_' + crypto.randomBytes(32).toString('hex'))"
VITE_MASTER_API_KEY=cfio_your_secure_master_key_here

# 🛠️ Development Settings
VITE_ENABLE_TESTNETS=true
VITE_ENABLE_PREMIUM_FEATURES=true